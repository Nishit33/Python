# How can you pick a random item from a range? 

import random                                   # import random number 

random_pick = random.choice(range(1,31))            # random pick variable = random.choice(range(between number ))
    
print(random_pick)                              # then print random pick show 
