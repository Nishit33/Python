# How will you compare two lists?

"""
So compare two list in python is 
You can compare two lists in Python using the equality operator == 
to check if they have the same elements in the same order.

list1 = [1, 2, 3]
list2 = [1, 2, 3]
list3 = [3, 2, 1]

print(list1 == list2)  # True, because both lists are the same
print(list1 == list3)  # False, because the order is different


"""